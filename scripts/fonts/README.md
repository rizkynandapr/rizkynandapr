Static instances used by `build_assets.py`, cut from the variable fonts:

- Archivo (wght 800, wdth 118) — SIL Open Font License 1.1, Omnibus-Type
- Geist (wght 500) — SIL Open Font License 1.1, Vercel
- Geist Mono (wght 500) — SIL Open Font License 1.1, Vercel

Each SVG embeds only the characters it uses.
